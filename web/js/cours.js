alert('Test envoi de données!');

// Création d'un objet FormData
//var identite = new FormData();
// Ajout d'information dans l'objet
//identite.append("login", "Boby the boss");
//identite.append("password", "123456");
// Création et configuration d'une requête HTTP POST vers le fichier post_form.php

//var req = new XMLHttpRequest();

//req.open("POST", "http://localhost/CatBot/web/app_dev.php/Test");

//req.send(identite);